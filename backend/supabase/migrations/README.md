# Migrations

**For a new Supabase project, run only [`../schema.sql`](../schema.sql)** — see [`../SETUP.md`](../SETUP.md).

The numbered files in this folder are **legacy** partial migrations from earlier development. They are kept for history but should not be applied on a fresh database:

| File | Status |
|------|--------|
| `002_intelligence_platform.sql` | Merged into `schema.sql` |
| `003_fields.sql` | Merged into `schema.sql` |
